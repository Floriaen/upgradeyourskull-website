---
interface Props {
  title: string;
  id?: string;
}
const { title, id } = Astro.props;
---

<h2
  id={id}
  class="text-2xl font-bold mb-6 text-[var(--color-text-primary)] pb-2 border-b border-[var(--color-border)]"
>
  {title}
</h2>
